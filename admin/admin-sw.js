const ADMIN_CACHE='yuran-admin-v7.4';
const ADMIN_SHELL=['/admin/','/admin/dashboard.html','/admin/admin.css?v=7.4','/admin/admin.js?v=7.4','/admin/manifest.webmanifest','/admin/icons/admin-192.png','/admin/icons/admin-512.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(ADMIN_CACHE).then(c=>c.addAll(ADMIN_SHELL).catch(()=>{})));self.skipWaiting()});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('yuran-admin-')&&k!==ADMIN_CACHE).map(k=>caches.delete(k)))));self.clients.claim()});
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==self.location.origin||url.pathname.startsWith('/api/'))return;if(req.mode==='navigate'){event.respondWith(fetch(req,{cache:'no-store'}).catch(()=>caches.match('/admin/dashboard.html')));return}if(url.pathname.startsWith('/admin/')){event.respondWith(fetch(req,{cache:'no-store'}).then(res=>{if(res.ok){const clone=res.clone();caches.open(ADMIN_CACHE).then(c=>c.put(req,clone))}return res}).catch(()=>caches.match(req)));}});
