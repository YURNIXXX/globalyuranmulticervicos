const grid=document.querySelector('#professionalsGrid'),form=document.querySelector('#searchForm'),input=document.querySelector('#search');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){grid.innerHTML='<p>A carregar...</p>';const r=await fetch('/api/professionals?q='+encodeURIComponent(input.value));const d=await r.json();grid.innerHTML=d.length?d.map(p=>`<a class="pro-card" href="/profissional/${encodeURIComponent(p.slug)}">${p.photo?`<img src="${esc(p.photo)}" alt="">`:`<div class="avatar-fallback"><i class="bi bi-person"></i></div>`}<div><small>${esc(p.location||'')}</small><h2>${esc(p.name)} ${p.verified?'<i class="bi bi-patch-check-fill"></i>':''}</h2><b>${esc(p.specialty||'Profissional')}</b><p>${esc((p.bio||'').slice(0,150))}</p><span>Ver perfil →</span></div></a>`).join(''):'<p>Ainda não há profissionais aprovados.</p>'}form.onsubmit=e=>{e.preventDefault();load()};load();
