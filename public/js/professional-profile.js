const root=document.querySelector('#profilePage');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const slug=decodeURIComponent(location.pathname.split('/').pop());fetch('/api/professionals/'+encodeURIComponent(slug)).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);return d}).then(({profile:p,services,projects})=>{root.innerHTML=`<section class="profile-hero">${p.photo?`<img class="profile-photo" src="${esc(p.photo)}">`:'<div class="profile-photo placeholder"><i class="bi bi-person"></i></div>'}<div><span>${esc(p.location||'')}</span><h1>${esc(p.name)} ${p.verified?'<i class="bi bi-patch-check-fill"></i>':''}</h1><h2>${esc(p.specialty||'Profissional')}</h2><p>${esc(p.bio||'')}</p><div class="profile-actions">${p.whatsapp?`<a href="https://wa.me/${encodeURIComponent(p.whatsapp.replace(/\D/g,''))}" target="_blank">WhatsApp</a>`:''}${p.cv_url?`<a href="${esc(p.cv_url)}" target="_blank">Ver CV</a>`:''}${p.website?`<a href="${esc(p.website)}" target="_blank">Website</a>`:''}</div></div></section><section class="profile-section"><h2>Serviços</h2><div class="service-public-grid">${services.map(s=>`<article><small>${esc(s.category)}</small><h3>${esc(s.title)}</h3><p>${esc(s.description)}</p><b>${esc(s.price)}</b></article>`).join('')||'<p>Nenhum serviço publicado.</p>'}</div></section><section class="profile-section"><h2>Projetos</h2><div class="project-public-grid">${projects.map(x=>`<article>${x.images?.[0]?`<img src="${esc(x.images[0])}">`:''}<div><h3>${esc(x.title)}</h3><p>${esc(x.description)}</p>${x.project_url?`<a href="${esc(x.project_url)}" target="_blank">Abrir projeto ↗</a>`:''}</div></article>`).join('')||'<p>Nenhum projeto publicado.</p>'}</div></section>`}).catch(e=>root.innerHTML=`<div class="not-found"><h1>Perfil indisponível</h1><p>${esc(e.message)}</p><a href="/profissionais">Ver profissionais</a></div>`);
