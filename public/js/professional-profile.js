const root=document.querySelector('#profilePage');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const slug=decodeURIComponent(location.pathname.split('/').pop());let payload=null;
function stars(avg=0){return `<span class="rating-stars" aria-label="${Number(avg).toFixed(1)} de 5">${[1,2,3,4,5].map(n=>`<i class="bi ${n<=Math.round(avg)?'bi-star-fill':'bi-star'}"></i>`).join('')}</span>`}
function contact(channel,serviceId=null,serviceTitle=''){if(!payload)return;fetch(`/api/professionals/${encodeURIComponent(slug)}/contact`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({channel,serviceId}),keepalive:true}).catch(()=>{});const p=payload.profile;if(channel==='whatsapp'){const text=serviceTitle?`Olá ${p.name}, gostaria de saber mais sobre o serviço: ${serviceTitle}.`:`Olá ${p.name}, encontrei o seu perfil na Yuran Multicerviços e gostaria de entrar em contacto.`;window.open(`https://wa.me/${String(p.whatsapp||'').replace(/\D/g,'')}?text=${encodeURIComponent(text)}`,'_blank','noopener')}else if(channel==='phone'){location.href=`tel:${p.phone}`}else if(channel==='email'){location.href=`mailto:${p.email}`}}
function render(){const {profile:p,services,projects,rating}=payload;document.title=`${p.name} | Yuran Multicerviços`;root.innerHTML=`<section class="profile-hero">${p.photo?`<img class="profile-photo" src="${esc(p.photo)}" alt="${esc(p.name)}">`:'<div class="profile-photo placeholder"><i class="bi bi-person"></i></div>'}<div><span>${esc(p.location||'')}</span><h1>${esc(p.name)} ${p.verified?'<i class="bi bi-patch-check-fill verified-icon"></i>':''}</h1><h2>${esc(p.specialty||'Profissional')}</h2><div class="rating-summary">${stars(rating.average)} <b>${Number(rating.average||0).toFixed(1)}</b> <span>(${rating.count} avaliação${rating.count===1?'':'ões'})</span></div><p>${esc(p.bio||'')}</p><div class="profile-actions">${p.whatsapp?`<button data-contact="whatsapp"><i class="bi bi-whatsapp"></i> WhatsApp</button>`:''}${p.phone?`<button data-contact="phone"><i class="bi bi-telephone"></i> Ligar</button>`:''}${p.email?`<button data-contact="email"><i class="bi bi-envelope"></i> E-mail</button>`:''}${p.cv_url?`<a href="${esc(p.cv_url)}" target="_blank" rel="noopener"><i class="bi bi-file-earmark-person"></i> Ver currículo</a>`:''}</div></div></section>
<section class="profile-section"><div class="section-inline-title"><div><span class="eyebrow">SERVIÇOS</span><h2>O que este profissional oferece</h2></div></div><div class="service-public-grid">${services.map(s=>`<article><small>${esc(s.category||'Serviço')}</small><h3>${esc(s.title)}</h3><p>${esc(s.description||'')}</p>${s.price?`<b>${esc(s.price)}</b>`:''}<button class="service-contact-btn" data-service-contact="${esc(s.id)}" data-service-title="${esc(s.title)}"><i class="bi bi-whatsapp"></i> Contactar sobre este serviço</button></article>`).join('')||'<p>Nenhum serviço publicado.</p>'}</div></section>
<section class="profile-section"><span class="eyebrow">PROJETOS</span><h2>Trabalhos publicados</h2><div class="project-public-grid">${projects.map(x=>`<article>${x.images?.[0]?`<img src="${esc(x.images[0])}" alt="${esc(x.title)}">`:''}<div><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p>${x.project_url&&x.project_url!=='#'?`<a href="${esc(x.project_url)}" target="_blank" rel="noopener">Abrir projeto ↗</a>`:''}</div></article>`).join('')||'<p>Nenhum projeto publicado.</p>'}</div></section>
<section class="profile-section contact-professional"><div><span class="eyebrow">CONTACTO DIRETO</span><h2>Fale com ${esc(p.name.split(' ')[0])}</h2><p>O contacto é feito diretamente com o profissional. A plataforma regista apenas o clique para estatísticas de desempenho.</p></div><div class="contact-buttons">${p.whatsapp?`<button data-contact="whatsapp"><i class="bi bi-whatsapp"></i> ${esc(p.whatsapp)}</button>`:''}${p.phone?`<button data-contact="phone"><i class="bi bi-telephone"></i> ${esc(p.phone)}</button>`:''}${p.email?`<button data-contact="email"><i class="bi bi-envelope"></i> ${esc(p.email)}</button>`:''}</div></section>
<section class="profile-section feedback-grid"><div class="feedback-card"><span class="eyebrow">AVALIAÇÃO</span><h2>Avalie a experiência</h2><p>Escolha atendimento geral ou um serviço específico.</p><form id="ratingForm" class="feedback-form"><select name="scopeKey"><option value="general">Atendimento geral</option>${services.map(s=>`<option value="${esc(s.id)}">${esc(s.title)}</option>`).join('')}</select><div class="star-picker">${[1,2,3,4,5].map(n=>`<label><input type="radio" name="stars" value="${n}" required><i class="bi bi-star-fill"></i><span>${n}</span></label>`).join('')}</div><textarea name="comment" rows="4" placeholder="Comentário opcional"></textarea><button>Enviar avaliação</button><p id="ratingMsg" class="form-message"></p></form>${rating.reviews?.length?`<div class="reviews"><h3>Últimas avaliações</h3>${rating.reviews.slice(0,5).map(r=>`<article>${stars(r.stars)}<p>${esc(r.comment||'Sem comentário.')}</p></article>`).join('')}</div>`:''}</div><div class="feedback-card report-card"><span class="eyebrow">SEGURANÇA</span><h2>Denunciar profissional</h2><p>Use esta opção para fraude, informação falsa, trabalho copiado, spam ou conduta imprópria.</p><form id="reportForm" class="feedback-form"><select name="reason" required><option value="">Motivo</option><option value="fraude">Suspeita de fraude</option><option value="informacao_falsa">Informação falsa</option><option value="conteudo_copiado">Trabalho/conteúdo copiado</option><option value="conduta">Conduta inadequada</option><option value="spam">Spam</option><option value="outro">Outro</option></select><textarea name="details" rows="5" placeholder="Descreva o que aconteceu" required></textarea><input name="reporterContact" placeholder="Seu e-mail/WhatsApp (opcional)"><button class="report-btn">Enviar denúncia</button><p id="reportMsg" class="form-message"></p></form></div></section>`;
 document.querySelectorAll('[data-contact]').forEach(b=>b.onclick=()=>contact(b.dataset.contact));document.querySelectorAll('[data-service-contact]').forEach(b=>b.onclick=()=>contact('whatsapp',b.dataset.serviceContact,b.dataset.serviceTitle));
 document.querySelector('#ratingForm').onsubmit=async e=>{e.preventDefault();const msg=document.querySelector('#ratingMsg');msg.textContent='A enviar...';try{const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(`/api/professionals/${encodeURIComponent(slug)}/rating`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error);msg.textContent='Avaliação registada. Obrigado.';setTimeout(()=>location.reload(),700)}catch(x){msg.textContent=x.message}};
 document.querySelector('#reportForm').onsubmit=async e=>{e.preventDefault();const msg=document.querySelector('#reportMsg');msg.textContent='A enviar...';try{const r=await fetch(`/api/professionals/${encodeURIComponent(slug)}/report`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const j=await r.json();if(!r.ok)throw new Error(j.error);msg.textContent='Denúncia enviada para revisão da administração.';e.currentTarget.reset()}catch(x){msg.textContent=x.message}};
}
fetch('/api/professionals/'+encodeURIComponent(slug)).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);return d}).then(d=>{payload=d;render()}).catch(e=>root.innerHTML=`<div class="not-found"><h1>Perfil indisponível</h1><p>${esc(e.message)}</p><a class="btn primary" href="/profissionais">Ver profissionais</a></div>`);
